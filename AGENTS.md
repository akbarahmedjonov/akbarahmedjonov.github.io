# Portfolio Repository

- Static site: single `index.html` with embedded CSS/JS. No build step.
- Dependencies: Bootstrap 5.3.2, Phosphor Icons, Google Fonts (all CDN).
- Assets: images in `/assets/`.
- Entry point: `index.html`.
